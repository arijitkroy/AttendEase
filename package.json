{
  "name": "employee-attendance-management-system",
  "version": "1.0.0",
  "description": "Full-stack Employee Attendance Management System (Node.js, Express, React, Firebase/Firestore)",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue.bold,green.bold\" \"npm run server\" \"npm run client\"",
    "start": "concurrently -n \"backend,frontend\" -c \"blue.bold,green.bold\" \"npm run server\" \"npm run client\"",
    "setup": "npm run install:all && npm run seed",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "server": "npm start --prefix backend",
    "client": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
